import { Box, Typography } from "@mui/material";

function Register() {
    return (
        <Box sx={{ mt: 2, textAlign: "center" }}>
            <Typography variant="h4">Register Sayfası</Typography>
        </Box>
    );
}
export default Register;